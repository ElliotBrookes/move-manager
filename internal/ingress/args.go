package ingress

import (
	"ElliotBrookes/move-manager/internal/domain"
)

func RequiredCliArgs([]string) (domain.MoveData, error) {
	// Serialize/ sanitise the input and check for required information
	//	vRegPtr := flag.String("r", "", "Vehicle Registration")
	//   payloadPtr :=
	//   todPtr :=
	//   fromPtr :=
	//   toPtr :=

	return domain.MoveData{},
		nil
}
